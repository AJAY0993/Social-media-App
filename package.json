{
  "name": "socilamediaapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd frontend && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd Backend && node server",
    "dev-start": "cd backend && nodemon server && cd frontend && npm run dev"
  },
  "author": "",
  "license": "ISC"
}
